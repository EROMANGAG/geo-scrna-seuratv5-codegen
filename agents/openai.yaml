interface:
  display_name: "GEO scRNA-seq Seurat v5 codegen"
  short_description: "Generate Seurat v5 R pipelines from GEO/SRA scRNA-seq datasets."
  default_prompt: "Use this skill to generate a Seurat v5 R processing script from the provided GEO/SRA scRNA-seq dataset."
policy:
  allow_implicit_invocation: true
